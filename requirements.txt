# Heavy stable deps. Pinned exactly so the pip install layer is reproducible
# and Docker Hub's layer cache survives across rebuilds.
#
# yt-dlp is intentionally NOT here — it's installed in a separate Dockerfile
# layer so its frequent updates don't invalidate the multi-GB whisperx layer.
# Bump via the YT_DLP_VERSION build-arg in the Dockerfile.
whisperx==3.8.5
# OCR for silent-video flow. Catches on-screen text (titles, credits,
# captions, lyrics) that the VLM either misses or hallucinates. Adds
# ~600 MB of model weights on first run (downloaded into HF cache).
# GPU-accelerated when CUDA available; falls back to CPU.
easyocr==1.7.2
# Backing store for /api/jobs (queue) and shared transcript cache.
# valkey-py is API-compatible with redis-py so the same URL scheme works.
valkey==6.1.1
# Web stack — Starlette ASGI app (API routes + SPA static mount) + uvicorn
# server + httpx client (live-transcription proxy). These were previously
# transitive via gradio; pinned explicitly now that the Gradio UI is retired.
starlette==1.3.1
uvicorn[standard]==0.49.0
httpx==0.28.1
# Backs Starlette's request.form() for the /api/upload + /api/image multipart
# endpoints (Starlette raises without it installed).
python-multipart==0.0.32
